# MomoPet Agent Execution Rules

本文件是仓库内所有本地 AI 编程软件、Codex、Claude Code、Cursor 或其他自动化执行工具的长期执行规则。

## 1. 角色边界

规划角色负责：
- 产品与技术规划
- 任务拆解
- 架构边界
- 验收标准
- Code Review
- 下一阶段决策

本地执行软件负责：
- 修改代码
- 执行命令
- 编译
- 测试
- 生成 diff
- 修复实现问题
- 汇报执行结果

除非任务文件明确要求，本地执行软件不得自行扩大任务范围。

## 2. 当前任务入口

每次执行前，必须先读取：

1. `AGENTS.md`
2. `tasks/CURRENT_TASK.md`

如果 `CURRENT_TASK.md` 指向其他任务文档，继续完整读取对应文档后再执行。

## 3. 通用禁止事项

除非 CURRENT_TASK 明确授权，禁止：
- 重写 UI
- 全面 MVVM 重构
- 大规模重命名
- 大面积格式化
- 迁移到 .NET 8
- 迁移到 Electron/Web
- 修改用户数据 schema
- 修改云端 API 协议
- 修改 AI Provider 行为
- 修改合规规则业务逻辑
- 修改 Messenger 业务协议
- 删除现有业务能力
- 提交 API Key、Token、AccessKey、用户数据、本机私有路径
- 提交 .agents、node_modules、临时构建目录

## 4. 执行原则

- 优先最小改动。
- 保持现有功能行为不变，除非任务明确要求改变。
- 修改前先理解相关代码路径，不允许盲改。
- 每个任务完成后必须 build。
- 存在对应测试时必须执行。
- 失败必须输出可诊断信息，不允许 silent failure。
- 不得用“测试未跑但应该没问题”作为完成结论。
- 不得自行跳过验收项。

## 5. Git 原则

开始执行前：
- 确认当前分支和工作区状态。
- 不覆盖用户已有未提交修改。
- 如果发现与当前任务无关的未提交修改，保留并明确报告。

执行完成后必须提供：
- 修改文件列表
- 每个文件的修改目的
- `git diff --stat`
- 关键 diff 摘要
- build 输出摘要
- test 输出摘要
- 未完成项
- 风险项
- 验收结论

## 6. 完成定义

只有满足 CURRENT_TASK 中全部“验收标准”时，才允许报告任务完成。

如果部分完成，必须明确写：
- 已完成
- 未完成
- 阻塞原因
- 建议下一步

不要隐藏失败，不要为了“看起来完成”而绕过检查。

## 7. 任务归档

当前任务完成并通过规划角色验收后，再由规划角色更新：
- `tasks/CURRENT_TASK.md`
- 将已完成任务归档到 `tasks/completed/`

本地执行软件不得擅自切换下一阶段任务。
